Répertoire
```cmd
cd JeuDesOrbites
```

Compilation
```cmd
javac -d .\build\classes\jeudesorbites\ .\src\jeudesorbites\*.java
```

Execution
```cmd
java -cp .\build\classes\ jeudesorbites.JeuDesOrbites
```